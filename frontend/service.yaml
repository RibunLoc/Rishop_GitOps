apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: rishop
spec:
  type: ClusterIP
  selector:
    app: frontend-deployment
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
