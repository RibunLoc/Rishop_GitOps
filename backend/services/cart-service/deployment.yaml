apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: rishop
  labels:
    app.kubernetes.io/name: cart-service
    app.kubernetes.io/part-of: rishop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        app: cart-service
        app.kubernetes.io/name: cart-service
        app.kubernetes.io/part-of: rishop
    spec:
      # affinity:
      #   nodeAffinity:
      #     requiredDuringSchedulingIgnoredDuringExecution:
      #       nodeSelectorTerms:
      #         - matchExpressions:
      #           - key: role
      #             operator: In
      #             values:
      #               - web-worker
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      terminationGracePeriodSeconds: 15
      imagePullSecrets:
        - name: ghcr-registry-secret
      containers:
        - name: cart-service
          image: ghcr.io/ribunloc/rishop-cart-service:4847199
          ports:
            - name: grpc
              containerPort: 50054
            - name: http
              containerPort: 8083
          envFrom:
            - configMapRef:
                name: rishop-backend-config
            - secretRef:
                name: rishop-backend-secret
          env:
            - name: GRPC_PORT
              value: "50054"
            - name: HTTP_PORT
              value: "8083"
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: rishop-backend-config
                  key: CART_POSTGRES_DB
            - name: REDIS_DB
              valueFrom:
                configMapKeyRef:
                  name: rishop-backend-config
                  key: CART_REDIS_DB
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
          resources:
            requests:
              memory: "32Mi"
              cpu: "25m"
            limits:
              memory: "64Mi"
              cpu: "100m"
