apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: rishop
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/part-of: rishop
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        app: auth-service
        app.kubernetes.io/name: auth-service
        app.kubernetes.io/part-of: rishop
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions: 
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - thanhloc
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      terminationGracePeriodSeconds: 15
      imagePullSecrets:
        - name: ghcr-registry-secret
      containers:
        - name: auth-service
          image: ghcr.io/ribunloc/rishop-auth-service:3beb398
          ports:
            - name: grpc
              containerPort: 50052
            - name: http
              containerPort: 8081
          envFrom:
            - configMapRef:
                name: rishop-backend-config
            - secretRef:
                name: rishop-backend-secret
          env:
            - name: GRPC_PORT
              value: "50052"
            - name: HTTP_PORT
              value: "8081"
            - name: POSTGRES_DB
              valueFrom:
                configMapKeyRef:
                  name: rishop-backend-config
                  key: AUTH_POSTGRES_DB
            - name: REDIS_DB
              valueFrom:
                configMapKeyRef:
                  name: rishop-backend-config
                  key: AUTH_REDIS_DB
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 3
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
